{
  "name": "instafollowx",
  "version": "1.0.0",
  "description": "InstaFollowX Platform",
  "scripts": {
    "install-deps": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev"
  },
  "dependencies": {}
} 
